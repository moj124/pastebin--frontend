import { addMinutesToDate } from "./addMinutesToDate";

test("addMinutesToDate returns a date, with a number of minutes onto a given date", () => {
//   expect(addMinutesToDate("World")).toBe("World");
//   expect(addMinutesToDate("Richard")).toBe("Richard");
//   expect(addMinutesToDate("Academy")).toBe("Academy");
//   expect(addMinutesToDate("academy")).toBe("Academy");
//   expect(addMinutesToDate("world")).toBe("World");
//   expect(addMinutesToDate("richard")).toBe("Richard");
});
